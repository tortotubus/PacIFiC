# set(DLMFD_INCLUDE_DIRS ${CMAKE_CURRENT_SOURCE_DIR}/DLMFD)
# add_library(dlmfd INTERFACE ${DLMFD_INCLUDE_DIRS})
# target_include_directories(dlmfd
#   INTERFACE
#     ${DLMFD_INCLUDE_DIRS}
# )
# target_link_libraries(dlmfd
#   INTERFACE
#     grains
# )

# set(VTK_HYPERTREE_INCLUDE_DIRS ${CMAKE_CURRENT_SOURCE_DIR}/VTKHyperTree)
# add_library(vtk_hypertree INTERFACE ${VTK_HYPERTREE_INCLUDE_DIRS})
# target_include_directories(vtk_hypertree
#   INTERFACE
#     ${VTK_HYPERTREE_INCLUDE_DIRS}
# )

# set(LAGRANGIAN_CAPS_INCLUDE_DIRS ${CMAKE_CURRENT_SOURCE_DIR}/lagrangian_caps)
# add_library(lagrangian_caps INTERFACE ${LAGRANGIAN_CAPS_INCLUDE_DIRS})
# target_include_directories(lagrangian_caps 
#   INTERFACE
#     ${LAGRANGIAN_CAPS_INCLUDE_DIRS}
# )

