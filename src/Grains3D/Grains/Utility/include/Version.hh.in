#pragma once

namespace Version {

// Numeric semantic version (CMake project version)
inline constexpr const char *semver = "@PROJECT_VERSION@";

// Full version string (tag + commit + dirty)
inline constexpr const char *full = "@PACIFIC_VERSION_FULL@";

// Git commit SHA (short or full)
inline constexpr const char *git_sha = "@PACIFIC_GIT_SHA@";

// Build timestamp (UTC)
inline constexpr const char *build_time = "@PACIFIC_BUILD_TIME@";

// CMake build type (Debug / Release / RelWithDebInfo)
inline constexpr const char *build_type = "@CMAKE_BUILD_TYPE@";

}